1. Go to the file directory
2. Open command prompt window there
3. And then install required modules by
	pip install -r requirements.txt
4. And then you can run both of bots
	python first.py
or
	python second.py